//! WASM exports for the path-geometry layer.
//!
//! Currently empty by design (ADR-F3-001). F5 will wire
//! `crate::route_geom::merge_route_facts` directly into
//! `wasm_finalize_plan` rather than exposing a separate wasm export here.
