//! wasm_api/tour_dto — placeholder; F5 will populate.
//!
//! At F2 time this module is intentionally empty: F2 ports the pure-data
//! `tour_dto` functions for Rust consumers. The `wasm_finalize_plan` exports
//! that compose these helpers into a JS-callable boundary live with F5.
//! See ADR-F2-008 (and ADR-F1-005 for the precedent established by
//! `wasm_api/heuristics.rs`).
